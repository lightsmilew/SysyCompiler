int func(int a, int b, float c, int d[5]) {
    return a + b;
}

int main() {
    int a = 1;
    int b = 2;
    float c = 3.0;
    int d[5] = {1, 2, 3, 4, 5};
    
    // 错误：函数参数类型不匹配
    func(a, b, a, d); // 错误：c是float类型，函数期望int类型
    
    // 正确调用
    return func(a, b, c, d);
}